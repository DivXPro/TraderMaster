// Prediction Constants
export const PREDICTION_DURATION = 30; // seconds
export const PREDICTION_PRICE_HEIGHT = 1; // price units
export const PREDICTION_GENERATION_INTERVAL = 30; // seconds (match duration for non-overlapping grid, or smaller for overlapping)
export const PREDICTION_LAYERS = 5; // number of layers up/down
export const PREDICTION_INITIAL_COLUMNS = 16; // number of columns to pre-generate
